/* initialize the bootstrap tooltip function */
$(function () { $("[data-toggle='tooltip']").tooltip(); });

/* initialize the bootstrap popover function */
$(function () { $("[data-toggle=popover]").popover({ trigger: "hover" }); });